<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- Add your EJS code here -->
    <% urls.forEach((url) => { %>
      <p><%= url.short %> : <%= url.long %></p>
    <% }) %>
</body>
</html>